# kustomize secretGenerator source — replace before build, not committed to main (add to .gitignore if needed)
# JWT_SECRET placeholder will be replaced by CI or `kubectl create secret`
JWT_SECRET=REPLACE_ME_openssl_rand_-hex_32
DATABASE_URL=postgresql://x402:REPLACE_ME@postgres:5432/x402_gateway
POSTGRES_PASSWORD=REPLACE_ME
