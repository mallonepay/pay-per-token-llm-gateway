apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: x402-ingress
  namespace: x402-gateway
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/limit-rps: "100"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - gateway.example.com  # TODO: replace with your domain; see overlays/
      secretName: x402-tls
  rules:
    - host: gateway.example.com  # TODO: replace
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 3000
