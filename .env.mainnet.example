[TEMPLATE]
# ═══════════════════════════════════════════════════════════════════
# x402 LLM Gateway — Mainnet Environment Configuration
# ═══════════════════════════════════════════════════════════════════
#
# Copy this file to .env and fill in your values:
#   cp .env.mainnet.example .env
#
# ⚠️  MAINNET — REAL USDC. Test everything on testnet first (Parts 1–4
#     of DEPLOYMENT.md). All secrets below must be replaced.
# ═══════════════════════════════════════════════════════════════════


# ──────────────────────────────────────────────
# 1. Server (Required)
# ──────────────────────────────────────────────

NODE_ENV=production
HOST=0.0.0.0
PORT=3000

# Public base URL used in payment quotes and instructions.
# Set this to your production gateway URL (e.g. https://x402-gateway.example.com)
# PUBLIC_GATEWAY_URL=https://x402-gateway.example.com


# ──────────────────────────────────────────────
# 2. Database & Redis (Required)
# ──────────────────────────────────────────────

# PostgreSQL connection string — use a production-grade host with TLS.
DATABASE_URL=postgresql://user:password@host:5432/x402_gateway

# Redis connection string — use a production-grade host with TLS + AOF persistence.
REDIS_URL=redis://host:6379


# ──────────────────────────────────────────────
# 3. Security (Required — fail-fast when missing)
# ──────────────────────────────────────────────

# JWT secret for dashboard session tokens — MUST be a random 256-bit value.
# Generate with: openssl rand -base64 32
JWT_SECRET=

# Session duration in seconds (default: 86400 = 24 hours)
# SESSION_DURATION=86400

# CORS origins — comma-separated list of allowed origins.
# Include your production dashboard URL.
CORS_ORIGINS=https://your-dashboard.vercel.app

# Express trust proxy setting. Used to resolve the real client IP behind
# Cloudflare/NGINX/Railway for IP-based rate limiting.
# TRUST_PROXY=1


# ──────────────────────────────────────────────
# 4. Stellar / Blockchain (Mainnet)
# ──────────────────────────────────────────────

STELLAR_NETWORK=mainnet
HORIZON_URL=https://horizon.stellar.org
SOROBAN_RPC_URL=https://soroban-mainnet.stellar.org

# Mainnet USDC issuer (Circle)
USDC_ISSUER=GA5ZSEJYB37JRC5AVCIA5MOP4RHTM335X2KGX3IHOJAPP5RE34K4KZVN


# ──────────────────────────────────────────────
# 5. Payment & Pricing
# ──────────────────────────────────────────────

# Quote expiry time in seconds (default: 300 = 5 minutes)
# QUOTE_EXPIRY_SECONDS=300

# Minimum payment amount in stroops (default 10000 = 0.001 XLM)
# MIN_PAYMENT_AMOUNT=10000

# Payment verification cache TTL in seconds (default: 3600 = 1 hour)
# PAYMENT_CACHE_TTL=3600


# ──────────────────────────────────────────────
# 6. On-Chain Settlement (Optional — Recommended for Mainnet)
# ──────────────────────────────────────────────

# Secret key of the contract admin Stellar account.
# Required for on-chain payment recording and escrow settlement on mainnet.
# Without this, the immutable on-chain audit trail is skipped.
# ⚠️  Store this in a secret manager, never in git.
# CONTRACT_ADMIN_SECRET=S...

# Opt-in per-token on-chain settlement via the credit-escrow contract.
# Requires CONTRACT_ADMIN_SECRET and an escrow contract funded by deposits.
# ESCROW_SETTLEMENT_ENABLED=true


# ──────────────────────────────────────────────
# 7. LLM / Upstream Configuration
# ──────────────────────────────────────────────

# Upstream LLM request timeout in milliseconds (default: 120000 = 2 min)
# LLM_REQUEST_TIMEOUT=120000

# Streaming request timeout in milliseconds (default: 600000 = 10 min)
# LLM_STREAM_TIMEOUT=600000

# Maximum retries for failed upstream calls (default: 2)
# LLM_MAX_RETRIES=2


# ──────────────────────────────────────────────
# 8. Provider API Keys
# ──────────────────────────────────────────────

# For each provider you register, set their upstream API key.
# Replace PROVIDER_ID with the UUID of the provider, uppercased,
# with hyphens replaced by underscores.
#
# Example: if provider ID is "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
#   UPSTREAM_API_KEY_A1B2C3D4_E5F6_7890_ABCD_EF1234567890=sk-your-api-key

# UPSTREAM_API_KEY_YOUR_PROVIDER_ID=sk-your-api-key-here


# ──────────────────────────────────────────────
# 9. Rate Limiting
# ──────────────────────────────────────────────

# Rate limit window in seconds (default: 60)
# RATE_LIMIT_WINDOW=60

# Max unpaid requests per window per IP (default: 10)
# RATE_LIMIT_MAX=10


# ──────────────────────────────────────────────
# 10. Deployed Mainnet Soroban Contract Addresses
# ──────────────────────────────────────────────
#
# Deploy your own mainnet contracts via:
#   STELLAR_NETWORK=mainnet STELLAR_SECRET_KEY=S... bash scripts/deploy-contracts.sh
#
# Then copy the addresses from contracts/deployed-addresses.json
# or set them here to override the defaults.

# PAYMENT_VERIFIER_CONTRACT=
# CREDIT_ESCROW_CONTRACT=
# MULTISIG_CONTRACT=


# ──────────────────────────────────────────────
# 11. Notifications (Optional)
# ──────────────────────────────────────────────

# Webhook notifications (enabled by default)
# WEBHOOK_ENABLED=true
# WEBHOOK_RETRY_COUNT=3
# WEBHOOK_RETRY_DELAY=1000
